from parse.gamma_parse import DataPre


ref_path = './screens_ref'
tar_path = './screens_tar'

# obj = DataPre('LinkJCExcel')
# obj.load_all_files(ref_path, tar_path)

# for ins in obj.instances[ref_path]:
    # print(ins.precision)

# 文件手动过滤
# obj = DataPre('LinkJCExcel')
# obj.load_all_files(ref_path, tar_path)
# obj.filter_by_shape(remove=True, printable=True)